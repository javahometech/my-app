name: hw workflow - Master
on:
    workflow_dispatch:
        inputs:
            environment:
                description: select the env
                required: true
                type: choice
                options:
                    - dev
                    - sit
                    - prod
jobs:
    say-hw:
        runs-on: ubuntu-latest
        env:
            dep_env:
                ${{ github.ref_name }}
        steps:
            - name: Say-hw
              run: echo "Deploying on ${dep_env}"
