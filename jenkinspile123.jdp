pipeline{
    agent any
    stages{
        stage('Run on PR'){
            when { changeRequest target: 'master' }.
                expression { env.CHANGE_ID ==~ /.*/ }
            }
            steps{
                echo "this is a pull request"
            }
        }
        stage('Merge'){
            steps([$class: 'GhprbPullRequestMerge'])
                
            }            }
        }
    }
}