node {
    checkout scm 
    /* .. snip .. */
    echo "${env.BRANCH_NAME}"
    stage('Deploy') {
            
            for(def branch: scm.branches){
              echo branch.name
            }
        }
}
