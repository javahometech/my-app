node {
    checkout scm 
    /* .. snip .. */
    stage('Deploy') {
            echo evn.BRANCH_NAME
            for(def branch: scm.branches){
              echo branch.name
            }
        }
}
