node {
    checkout scm 
    /* .. snip .. */
    stage('Deploy') {
            echo "${BRANCH_NAME}"
            for(def branch: scm.branches){
              echo branch.name
            }
        }
}
